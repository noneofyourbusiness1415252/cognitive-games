{
  "author": "You <you@example.com>",
  "name": "rust-yew-ssr-template",
  "version": "0.1.0",
  "scripts": {
    "build": "cargo build --release && wrangler build",
    "start": "wrangler dev",
    "prepare": "cargo build --release",
    "test": "cargo test"
  },
  "type": "module",
  "devDependencies": {}
}
